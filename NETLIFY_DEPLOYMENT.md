# 🚀 دليل النشر على Netlify - EDUSPARK

## 📍 معلومات النشر الحالي

- **🌐 رابط الموقع**: https://eduspark-s.netlify.app/
- **📦 المنصة**: Netlify
- **🔧 الإطار**: Next.js 16.0.1
- **📊 الحالة**: ✅ نشط

---

## ⚙️ متغيرات البيئة المطلوبة

يجب إضافة المتغيرات التالية في **Site settings** → **Environment variables** على Netlify:

### 1. قاعدة البيانات
```
DATABASE_URL = "postgresql://user:password@host:5432/database"
```

### 2. المصادقة (NextAuth)
```
NEXTAUTH_URL = "https://eduspark-s.netlify.app"
NEXTAUTH_SECRET = "قم بتوليد secret باستخدام: openssl rand -base64 32"
```

### 3. الجلسة
```
SESSION_SECRET = "قم بتوليد secret آخر"
```

### 4. إعدادات Node.js
```
NODE_VERSION = "20"
NPM_FLAGS = "--legacy-peer-deps"
```

---

## 🔄 تحديث الموقع

عند القيام بأي تغييرات:

1. **اختبر محلياً**:
```bash
npm run build
npm run start
```

2. **ارفع التغييرات**:
```bash
git add .
git commit -m "وصف التغييرات"
git push origin main
```

3. **Netlify سيقوم بالنشر تلقائياً** (عادة 2-5 دقائق)

---

## 📋 التحقق من الأخطاء

إذا فشل البناء على Netlify:

1. اذهب إلى **Deploys** في لوحة Netlify
2. افتح آخر عملية نشر فاشلة
3. تحقق من **Deploy log** لرؤية الأخطاء
4. الأخطاء الشائعة:
   - ❌ متغيرات البيئة غير موجودة
   - ❌ أخطاء في البناء (TypeScript errors)
   - ❌ تبعيات ناقصة في package.json

---

## 🎯 تحسينات مستقبلية

### 1. إضافة Domain مخصص
- اذهب إلى **Domain management** في Netlify
- أضف النطاق الخاص بك
- حدّث DNS records

### 2. تفعيل HTTPS
- Netlify توفر SSL تلقائياً
- تأكد من تفعيل **Force HTTPS**

### 3. إعداد Caching
- الإعدادات الحالية في `netlify.toml` محسّنة بالفعل

### 4. مراقبة الأداء
- استخدم **Analytics** في لوحة Netlify
- راقب **Functions** usage

---

## 📞 الدعم

- **Netlify Docs**: https://docs.netlify.com
- **Next.js on Netlify**: https://docs.netlify.com/frameworks/next-js/
- **المستودع**: https://github.com/Tr-Abdullah/eduspark

---

✨ **آخر تحديث**: نوفمبر 2025
